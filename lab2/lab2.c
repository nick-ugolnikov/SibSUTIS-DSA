#include "bstree.h"
#include <stdlib.h>
#include <stdio.h>

int main(int argc, char *argv[])
{
	int mode = atoi(argv[1]);

	switch (mode)
	{
		case 1:
			struct bstree *baum = bstree_create();
			break;
		case 2;
			break;
	}
}